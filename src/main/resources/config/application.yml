spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/outgo
    username: {username}
    password: {password}
doma:
  dialect: mysql
  sql-file-repository: no_cache