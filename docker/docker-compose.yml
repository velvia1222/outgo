version: '3.1'
services:
    db:
        image: postgres:9
        restart: always
        environment:
            POSTGRES_PASSWORD: postgres
        volumes:
            - ./postgres/initdb.d:/docker-entrypoint-initdb.d
        ports:
            - "5432:5432"
